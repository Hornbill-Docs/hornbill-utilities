## Scheduling Overview

### Windows
You can schedule this utility to run with any optional command line argument from Windows Task Scheduler.

- Ensure the user account running the task has rights to run the utility and the containing folder as well as other folders involved.
- Make sure the `Start In`-parameter contains the folder where the utility resides in - otherwise it will not be able to pick up the correct path.
